---
- name: Install neofetch
  hosts: debian
  gather_facts: true

  tasks:
    - name: Install neofetch
      ansible.builtin.apt:
        name: neofetch
        state: present
        update_cache: true